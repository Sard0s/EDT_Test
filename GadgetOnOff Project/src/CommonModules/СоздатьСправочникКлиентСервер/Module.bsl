
&НаСервере
Функция ПроверкаЭлемента(Наименование, НазваниеСправочника, ЭтоНовый) Экспорт 
	
	Отказ = Ложь;
	Если ЭтоНовый Тогда	
		НайденнаяНоменклатура = Справочники[НазваниеСправочника].НайтиПоНаименованию(Наименование);	
		Если НЕ НайденнаяНоменклатура.Пустая() Тогда		
			Отказ = Истина;	
			Если Отказ = Истина Тогда	
				Сообщить("Элемент справочника уже создан в системе!");		
			КонецЕсли;	
		КонецЕсли;	
	КонецЕсли;
	Возврат Отказ;

КонецФункции
	